diff --git a/docker/edgeric-v2/protobufs/metrics.proto b/docker/edgeric-v2/protobufs/metrics.proto
index d74c0da..e6eada7 100644
--- a/docker/edgeric-v2/protobufs/metrics.proto
+++ b/docker/edgeric-v2/protobufs/metrics.proto
@@ -9,6 +9,7 @@ message UeMetrics {
     uint32 dl_buffer = 6;
     uint32 ul_buffer = 7;
     float dl_tbs = 8;
+    optional bool ul_harq_ack = 20;  // true = ACK (CRC OK), false = NACK
 }
 
 message Metrics {
diff --git a/docker/radio_network/srs-4G-UE/srsgnb/src/stack/mac/mac_nr.cc b/docker/radio_network/srs-4G-UE/srsgnb/src/stack/mac/mac_nr.cc
index 6c25743..538dc1a 100644
--- a/docker/radio_network/srs-4G-UE/srsgnb/src/stack/mac/mac_nr.cc
+++ b/docker/radio_network/srs-4G-UE/srsgnb/src/stack/mac/mac_nr.cc
@@ -629,6 +629,9 @@ int mac_nr::pusch_info(const srsran_slot_cfg_t& slot_cfg, mac_interface_phy_nr::
 
   sched->ul_crc_info(rnti, 0, pusch_info.pid, pusch_info.pusch_data.tb[0].crc);
 
+  // push to edgeric: true = ACK, false = NACK
+  edgeric::set_ul_harq_ack(rnti, pusch_info.pusch_data.tb[0].crc);
+
   // process only PDUs with CRC=OK
   if (pusch_info.pusch_data.tb[0].crc) {
     if (pcap) {
diff --git a/docker/radio_network/srsRAN-5G-ER/lib/edgeric/edgeric.cpp b/docker/radio_network/srsRAN-5G-ER/lib/edgeric/edgeric.cpp
index b4f6e54..5908e3a 100644
--- a/docker/radio_network/srsRAN-5G-ER/lib/edgeric/edgeric.cpp
+++ b/docker/radio_network/srsRAN-5G-ER/lib/edgeric/edgeric.cpp
@@ -12,6 +12,7 @@ std::map<uint16_t, float> edgeric::tx_bytes = {};
 std::map<uint16_t, uint32_t> edgeric::ue_ul_buffers = {};
 std::map<uint16_t, uint32_t> edgeric::ue_dl_buffers = {};
 std::map<uint16_t, float> edgeric::dl_tbs_ues = {};
+std::map<uint16_t, bool> edgeric::ul_harq_ack = {};
 
 // std::map<uint16_t, float> edgeric::weights_recved = {};
 std::map<uint16_t, float> edgeric::weights_recved = {};
@@ -33,6 +34,10 @@ zmq::socket_t publisher(context, ZMQ_PUB);
 zmq::socket_t subscriber_weights(context, ZMQ_SUB);
 zmq::socket_t subscriber_mcs(context, ZMQ_SUB);
 
+void edgeric::set_ul_harq_ack(uint16_t rnti, bool ack) {
+    ul_harq_ack[rnti] = ack;
+}
+
 void edgeric::init() {
     // publisher.bind("ipc:///tmp/metrics");
     publisher.bind("tcp://10.53.2.4:5050");
@@ -101,6 +106,11 @@ void edgeric::send_to_er() {
         // Set UL Buffer, default to 0 if not available
         auto dl_tbs_it = dl_tbs_ues.find(rnti);
         ue_metrics->set_dl_tbs((dl_tbs_it != dl_tbs_ues.end()) ? dl_tbs_it->second : 0);
+
+        auto ack_it = ul_harq_ack.find(rnti);
+        if (ack_it != ul_harq_ack.end()) {
+            ue_metrics->set_ul_harq_ack(ack_it->second);
+        }
     }
 
     // Serialize the Metrics message to a string
@@ -122,6 +132,7 @@ void edgeric::send_to_er() {
     tx_bytes.clear();
     rx_bytes.clear();
     dl_tbs_ues.clear();
+    ul_harq_ack.clear();
     // ue_dl_buffers.clear();
     // ue_ul_buffers.clear();
 }
diff --git a/docker/radio_network/srsRAN-5G-ER/lib/edgeric/edgeric.h b/docker/radio_network/srsRAN-5G-ER/lib/edgeric/edgeric.h
index 30e25ef..d04f4b9 100644
--- a/docker/radio_network/srsRAN-5G-ER/lib/edgeric/edgeric.h
+++ b/docker/radio_network/srsRAN-5G-ER/lib/edgeric/edgeric.h
@@ -27,6 +27,9 @@ private:
     static std::map<uint16_t, uint32_t> ue_ul_buffers;
     static std::map<uint16_t, uint32_t> ue_dl_buffers;
     static std::map<uint16_t, float> dl_tbs_ues;
+    
+    static std::map<uint16_t, bool> ul_harq_ack;
+    static void set_ul_harq_ack(uint16_t rnti, bool ack);
 
     static uint32_t er_ran_index_weights;
     static uint32_t er_ran_index_mcs;
diff --git a/docker/radio_network/srsRAN-5G-ER/lib/protobufs/metrics.proto b/docker/radio_network/srsRAN-5G-ER/lib/protobufs/metrics.proto
index 03e5a0c..69168a0 100644
--- a/docker/radio_network/srsRAN-5G-ER/lib/protobufs/metrics.proto
+++ b/docker/radio_network/srsRAN-5G-ER/lib/protobufs/metrics.proto
@@ -9,6 +9,7 @@ message UeMetrics {
     uint32 dl_buffer = 6;
     uint32 ul_buffer = 7;
     float dl_tbs = 8;
+    optional bool ul_harq_ack = 20;  // true = ACK (CRC OK), false = NACK
 }
 
 message Metrics {
diff --git a/edgeric-v2/protobufs/metrics.proto b/edgeric-v2/protobufs/metrics.proto
index d74c0da..2068540 100644
--- a/edgeric-v2/protobufs/metrics.proto
+++ b/edgeric-v2/protobufs/metrics.proto
@@ -9,6 +9,8 @@ message UeMetrics {
     uint32 dl_buffer = 6;
     uint32 ul_buffer = 7;
     float dl_tbs = 8;
+    optional bool ul_harq_ack = 20;  // true = ACK (CRC OK), false = NACK
+
 }
 
 message Metrics {
diff --git a/srsRAN-5G-ER/lib/protobufs/metrics.proto b/srsRAN-5G-ER/lib/protobufs/metrics.proto
index 03e5a0c..20eb83a 100644
--- a/srsRAN-5G-ER/lib/protobufs/metrics.proto
+++ b/srsRAN-5G-ER/lib/protobufs/metrics.proto
@@ -9,6 +9,8 @@ message UeMetrics {
     uint32 dl_buffer = 6;
     uint32 ul_buffer = 7;
     float dl_tbs = 8;
+    optional bool ul_harq_ack = 20;  // true = ACK (CRC OK), false = NACK
+
 }
 
 message Metrics {
